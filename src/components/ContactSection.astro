---
const contacts: any = 
    [
        {
            image: 'discord',
            copy: 'qqbn_',
            link: null,
            text: 'Hit me up on Discord!'

        }, 
        {
            image: 'email',
            copy: 'jkfrontdev@gmail.com',
            link:  'jkfrontdev@gmail.com',
            text: 'Reach out via email!',
        }, 
        {
            image: 'linkedin',
            copy: null,
            link: 'https://www.linkedin.com/in/jkfrontdev/',
            text: 'Check me out on LinkedIn!'
        }
    ];
---
<script>
    const btns = document.querySelectorAll('.copy-btn');
    
    btns.forEach(btn => {
        const value = btn.getAttribute('data-copy');
        const name = btn.getAttribute('data-name');
        if(value && name){
            btn.addEventListener('click', () => copyToClipboard(value, name));
        }
    })

    const copyToClipboard = (value: string, name: string) => {
        navigator.clipboard.writeText(value);
        const tooltip = document.getElementById('tooltip');
        if(!tooltip) return;

        tooltip.classList.add('show');
        tooltip.textContent = `${name} copied to clipboard!`;
        setTimeout(() => tooltip?.classList.remove('show'), 2000);
    };
</script>

<section class="contact-section montserrat-regular" id="contact">
    <h2 class="montserrat-alternates-bold text-gradient-purple">Contact</h2>
    <div class="contact-section-content-wrapper">
        <p class="montserrat-regular">Below, you'll find various ways to get in touch with me.</p>
    
        <div class="contact-section-images-wrapper">
                {contacts.map((contact: any, index: number) => (
                    <div class={`image-box image-box${index+1}`} data-aos="fade-right">
                        <img src={`/src/assets/images/${contact.image}.png`} alt={`${contact.image.toUpperCase()} image`} loading="lazy" />
                        <div class="image-box-buttons">
                            <p class="image-box-text">{contact.text}</p>
                            {contact.copy ? (<button type="button" class="btn montserrat-regular copy-btn" data-copy={contact.copy} data-name={contact.image}>copy</button>) : ('')}
                            {contact.link ? <a type="button" class="btn montserrat-regular" href={contact.image==='email' ? `mailto:${contact.link}` : `${contact.link}`} target="_blank">go to</a> : ('')}
                        </div>
                   </div> 
                ))}
        </div>

        <p class="montserrat-bold text-gradient-light-purple greetings-text">I look forward to hearing from you soon!</p>
    </div>

    <div id="tooltip" class="tooltip blob"></div>
</section>
<style lang="scss" scoped>
    .contact-section{
        background-color: var(--light-black);
        width: 100%;
        color: var(--white);
        padding: 2rem 4rem;
        display: flex;

        justify-content: flex-start;
        align-items: flex-start;
        flex-direction: column;
        h2{
            font-size: 2rem;
        }

        .contact-section-content-wrapper{ 
            width: 100%;
            display: flex;
            justify-content: center;
            align-items: center;
            flex-direction: column;
            gap: 40px;

            .greetings-text{
                font-size: 26px;
            }
            p{
                max-width: 800px;
                font-size: 20px;
                text-align: center;
            }
        }

        .contact-section-images-wrapper{
            display: flex;
            flex-direction: column;
            width: 100%;

            .image-box{
                width: 100%;
                max-width: 600px;
                height: auto;
                background-color: #1c1f23;
                margin-top: -20px;
                position: relative;
                padding: 1.5rem;
                display: flex;
                justify-content: flex-start;
                align-items: center;
                border-radius: 1rem;
                box-shadow: rgba(0, 0, 0, 0.16) 0px 10px 36px 0px, rgba(0, 0, 0, 0.06) 0px 0px 0px 1px;
                gap: 20px;

                .image-box-buttons{
                    display: flex;
                    justify-content: center;
                    align-items: center;
                    flex-direction: column;
                    gap: 20px;
                    .image-box-text{
                        max-width: 300px;
                    }
                }
            }

            .image-box:nth-child(1) {
                margin-left: 5%;
            }

            .image-box:nth-child(2) {
                margin-left: calc(50% - 300px);
            }

            .image-box:nth-child(3) {
                margin-left: calc(95% - 600px);
            }

            @media (max-width: 1024px) {
                gap: 20px;
                .image-box{
                    width: 80%;
                    margin-left: auto;
                    margin-right: auto;
                    display: flex;
                    justify-content: center;
                    align-items: center;
                    flex-direction: column;
                    gap: 20px;
                    margin-top: 0;
                }

                .image-box:nth-child(1), .image-box:nth-child(2), .image-box:nth-child(3) {
                    margin-left: auto;
                    margin-right: auto;
                }
            }
        }
        img{
            max-width: 300px;
            height: auto;
        }
    }

    .tooltip {
    display: none;
    position: fixed;
    top: 30%;
    left: 40%;
    background-color: #333;
    color: #fff;
    text-align: center;
    padding: 40px;
    border-radius: 15px;
    transform: scale(0);
    transition: transform 0.3s ease;
  }

  .tooltip.show {
    display: flex;
    transform: scale(1);
  }

  .blob {
    background: var(--gradient-purple-background);
    width: 300px;
    height: 150px;
    color: white;
    padding: 20px;
    border-radius: 61% 39% 38% 62% / 58% 43% 57% 42%;
    display: flex;
    justify-content: center;
    align-items: center;
    font-size: 20px;
    text-align: center;
    display: flex;
    box-shadow: rgba(0, 0, 0, 0.16) 0px 10px 36px 0px, rgba(0, 0, 0, 0.06) 0px 0px 0px 1px;

  }
</style>