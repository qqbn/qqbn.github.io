import { defineConfig } from 'astro/config';

export default defineConfig({
  site: 'https://username.github.io',
  // TODO: uzupełnij - zmień "repo-name" na nazwę swojego repozytorium GitHub
  // Jeśli wdrażasz na domenę główną (username.github.io), ustaw base: '/'
  base: '/',
});
